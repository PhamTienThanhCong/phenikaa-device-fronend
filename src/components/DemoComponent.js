import { APP_NAME } from '@/constants/app';

const DemoComponent = () => {
  return (
    <div>
      <h1>Hello, World!</h1>
      <p>This is a demo component. {APP_NAME}</p>
    </div>
  );
};

export default DemoComponent;
